import Model from './Model.js'

export default class Plan extends Model {
  constructor(name, workouts, user) {
    super()
    this.TAG = `[ Plan ]`
    this.name = name
    // workouts
    // user
  }
}